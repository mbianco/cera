# Docs — Build and deploy to GitHub Pages
#
# Builds the MkDocs site and deploys to GitHub Pages using the
# official actions/deploy-pages action.
#
# IMPORTANT: For this workflow to deploy, GitHub Pages must be
# configured to use "GitHub Actions" as the source (not "Deploy
# from a branch"). Go to:
#   Settings → Pages → Build and deployment → Source → "GitHub Actions"
#
# Runs on every push to main that changes files in docs/ or
# mkdocs.yml, plus on workflow_dispatch.

name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build docs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install MkDocs
        run: pip install mkdocs

      - name: Build site
        run: mkdocs build --strict

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
