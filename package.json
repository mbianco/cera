{
  "name": "cera",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI agent for climate scientists on the Alps supercomputer, built on DeepSeek Harness (dsh)",
  "license": "MIT",
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "test:fast": "vitest run --reporter=verbose",
    "lint": "eslint src tests --max-warnings=0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/node": "^22.7.0",
    "eslint": "^9.12.0",
    "fast-check": "^4.10.1",
    "tsdown": "^0.6.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.8.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "overrides": {}
  }
}
